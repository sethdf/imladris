summary: 'Approve and execute a pending remediation (playbook → verify → Slack confirm)'
description: 'Seth runs this to approve or reject a pending remediation. Executes playbook, verifies, posts Slack confirmation.'
lock: '!inline f/devops/approve_remediation.script.lock'
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    action:
      type: string
      description: ''
      default: approve
      originalType: string
    remediation_id:
      type: string
      description: ''
      default: null
      originalType: string
  required:
    - remediation_id
