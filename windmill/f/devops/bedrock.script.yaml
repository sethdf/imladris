summary: 'Shared Bedrock API helper for pipeline AI calls'
description: 'Wraps AWS Bedrock invoke-model via CLI. Replaces claude -p with direct Bedrock calls.'
lock: '!inline f/devops/bedrock.script.lock'
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties: {}
  required: []
