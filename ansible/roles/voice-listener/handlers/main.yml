---
- name: restart voice-listener
  ansible.builtin.systemd:
    name: voice-listener
    scope: user
    state: restarted
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ voice_listener_uid }}"
    DBUS_SESSION_BUS_ADDRESS: "unix:path=/run/user/{{ voice_listener_uid }}/bus"
  failed_when: false
