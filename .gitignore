# Secrets
.env
*.pem
*.key

# Terraform (v1 remnants — should not exist)
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl
tfplan

# Dependencies
node_modules/

# OS
.DS_Store
*.swp
*~

# Not part of imladris — tracked in separate repos
adeptia/
finops/

# Runtime state (should never be in this repo)
MEMORY/
*.jsonl

# Windmill CLI / AI agent config (auto-generated)
windmill/wmill.yaml
windmill/wmill-lock.yaml
windmill/.claude/
windmill/CLAUDE.md
windmill/AGENTS.md

# modules/pai — defense-in-depth: never commit runtime/secrets/personal
modules/pai/settings.json
modules/pai/**/.env
modules/pai/**/*.key
modules/pai/**/*.pem
modules/pai/**/.credentials.json
modules/pai/USER/
modules/pai/TELOS/
