# Terraform
.terraform/
*.tfplan
crash.log
crash.*.log

# State files are committed (encrypted via git-crypt)
# Do NOT ignore: terraform.tfstate, terraform.tfstate.backup

# Sensitive files - use sops secrets.yaml instead
terraform.tfvars
.secrets.auto.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Decrypted secrets (temporary)
secrets.yaml.tmp
secrets.decrypted.yaml

# Sops age recipient (not secret, but local)
.sops-age-recipient

# SSH keys
*.pem

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
.bitwarden/
nix/result
